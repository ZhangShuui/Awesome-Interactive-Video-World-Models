# arXiv ids the candidate pipeline should stop proposing.
# One id per line; everything after # is a comment.
#
# Use this for papers that keep matching the query but will never belong --
# not for papers you merely have not reviewed yet. Those come back on their
# own in the next inbox refresh, which is the point.
#
# That distinction now costs more than it used to. An unreviewed candidate is
# carried across window boundaries instead of ageing out of the inbox, so an
# unwanted match no longer leaves on its own after three days: it returns every
# day until it is listed here.
#
# Example:
# 2405.12345  # world model for supply-chain forecasting, no pixels

2608.14391  # detecting AI-generated crisis footage -- about video forensics, not world models
2608.13925  # CForce: parallel decoding for dLLMs; matched "consistency forcing", no pixels
2608.13112  # physics-faithful scientific diagrams -- static figures, no rollout
